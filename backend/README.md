Initiated vertual env:
goto ~/backend then run-- $source venv/bin/activate
Then run -- python app.py
By running above it will not fetch/show tls-sessions in UI: temp fix -- $sudo chmod +r /run/openvpn/server.status

